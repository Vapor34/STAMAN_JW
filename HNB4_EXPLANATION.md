# hnb4.py 最终优化 - Shadow Hand 执行器限制解析

## 关键发现

**Shadow Hand 的 XML 配置中，只为部分关节定义了执行器：**

```
有执行器的关节（可直接控制）：
  ✓ J3（远端指节）- 4 个执行器（FFJ3, MFJ3, RFJ3, LFJ3）
  ✓ J4/Abd（侧摆）- 4 个执行器
  ✓ J5（小指基部）- 1 个执行器
  ✓ 大拇指 - 5 个执行器
  ✓ 腕部 - 2 个执行器

没有执行器的关节（被动关节）：
  ✗ J1（近端指节）- 无执行器
  ✗ J2（中间指节）- 无执行器
```

## 设计意图

这是 Shadow Hand 的**链式驱动设计**：
- **J3是主动驱动**：通过执行器直接弯曲远端指节
- **J1/J2被动跟随**：通过物理接触、弹簧和几何约束被J3推动

这样做的好处：
- 减少执行器数量，降低成本
- 实现自然的手指弯曲链条（先J3，然后J1和J2依次跟随）
- 提高抓握的刚性和力感

## hnb4.py 的改进方案

既然J1和J2无法直接控制，新策略是**集中所有驱动力在J3上，让J3充分弯曲，从而推动J1和J2**：

```python
# 旧策略（分级激活不可行）
if jnt_adr in flex_j1_indices:  # ✗ 没执行器
    val = ...
elif jnt_adr in flex_j2_indices:  # ✗ 没执行器
    val = ...
elif jnt_adr in flex_j3_indices:  # ✓ 有执行器
    val = ...

# 新策略（集中驱动）
if jnt_adr in flex_j3_indices:  # ✓ 只控制J3
    val = (grasp_val ** 1.0) * 2.5 * 3.5  # 最大力度 8.75
```

## 参数说明

```python
grasp_force_factor = 3.5  # 过压系数
基础系数 = 2.5
非线性指数 = 1.0

最大 J3 力度 = 2.5 * 3.5 = 8.75
（执行器范围通常是 [-1, 1]，所以会被裁剪到 1.0）
```

## 预期效果

当 `grasp_val` 从 0 → 1 时：

1. **J3 快速弯曲**：执行器输出最大力（1.0）
2. **J1 跟随弯曲**：被J3物理推动
3. **J2 跟随弯曲**：被J1和J3推动
4. **最终握拳**：自然的五指闭合姿态

这比之前的"只有J1弯曲"要好得多，因为现在J3能充分弯曲，从而拉动整个链条。

## 注意事项

- J3 力度的上限由执行器的 `ctrlrange` 决定（通常 [-1, 1]）
- 如果J1/J2仍然不够弯曲，可能是XML中的关节弹簧/阻尼设置问题
- 可以尝试调整 `grasp_force_factor`（3.5 → 4.0 或更高）

## 技术细节

```
执行器列表分析：
执行器ID | 目标关节 | 备注
─────────────────────────────
0-1      | WRJ1/2   | 腕部
2-6      | THJ1-5   | 大拇指
7-8      | FFJ4, FFJ3 | 食指
9        | palm_freejoint | ✗ 无效
10-11    | MFJ4, MFJ3 | 中指
12       | WRJ2 (重复) | ✗ 错误配置
13-14    | RFJ4, RFJ3 | 无名指
15       | WRJ1 (重复) | ✗ 错误配置
16-18    | LFJ5, LFJ4, LFJ3 | 小指
19       | FFJ4 (重复) | ✗ 错误配置
```

注：XML 中某些执行器配置似乎有重复或错误，但这不影响主要功能。
